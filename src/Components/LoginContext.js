import React,{useContext} from 'react';
import signContext from './Components/Signin'

export default function LoginContext() {
    let a = useContext(signContext);
    let arr =[];
  return (
    <div>
        
    </div>
  );
}
